<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdkj.core.dao.IInteractDAO">
	<resultMap id="interact_DB_Result" type="interact">
		<result column="code" property="code" />
		<result column="type" property="type" />
		<result column="kind" property="kind" />
		<result column="entity_code" property="entityCode" />
		<result column="to_user" property="toUser" />
		<result column="interacter" property="interacter" />
		
		<result column="interact_datetime" property="interactDatetime" />
		<result column="quantity" property="quantity" />
		<result column="remark" property="remark" />
		<result column="company_code" property="companyCode" />
		<result column="system_code" property="systemCode" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
			<if test="type != null and type != '' ">
				AND t.type = #{type}
			</if>
			<if test="kind != null and kind != '' ">
				AND t.kind = #{kind}
			</if>
			<if test="entityCode != null and entityCode != '' ">
				AND t.entity_code = #{entityCode}
			</if>
			<if test="toUser != null and toUser != '' ">
				AND t.to_user = #{toUser}
			</if>
			<if test="interacter != null and interacter != '' ">
				AND t.interacter = #{interacter}
			</if>
			
			<if test="interactDatetimeStart != null and interactDatetimeStart != '' ">
				<![CDATA[AND t.interact_datetime >= #{interactDatetimeStart}]]>
			</if>
			<if test="interactDatetimeEnd != null and interactDatetimeEnd != '' ">
				<![CDATA[AND t.interact_datetime <= #{interactDatetimeEnd}]]>
			</if>
			<if test="companyCode != null and companyCode != '' ">
				AND t.company_code = #{companyCode}
			</if>
			<if test="systemCode != null and systemCode != '' ">
				AND t.system_code = #{systemCode}
			</if>
		</trim>
	</sql>
	<select id="select_interact" parameterType="interact" resultMap="interact_DB_Result">
		SELECT
		t.code
		,t.type
		,t.kind
		,t.entity_code
		,t.to_user
		,t.interacter
		,t.interact_datetime
		,t.quantity
		,t.remark
		,t.company_code
		,t.system_code
		FROM
		tstd_interact t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_interact_count" parameterType="interact"
		resultType="java.lang.Long">
		SELECT count(1) FROM tstd_interact t
		<include refid="where_condition" />
	</select>

	<insert id="insert_interact" parameterType="interact">
		INSERT INTO
		tstd_interact(
		code
		,type
		,kind
		,entity_code
		,to_user
		,interacter
		,interact_datetime
		,quantity
		,remark
		,company_code
		,system_code
		)
		VALUES(
		#{code}
		,#{type}
		,#{kind}
		,#{entityCode}
		,#{toUser}
		,#{interacter}
		,#{interactDatetime}
		,#{quantity}
		,#{remark}
		,#{companyCode}
		,#{systemCode}
		)
	</insert>

	<delete id="delete_interact" parameterType="interact">
		DELETE FROM tstd_interact
		WHERE code = #{code}
	</delete>
	
	<delete id="delete_interactByEntityCode" parameterType="interact">
		DELETE FROM tstd_interact
		WHERE entity_code = #{entityCode}
	</delete>
	
	<select id="select_totalQuantity" parameterType="interact"
		resultType="java.lang.Long">
		SELECT ifnull(sum(quantity),0) FROM tstd_interact t 
		<include refid="where_condition" />
	</select>
</mapper>